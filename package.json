{
  "name": "limit-number-of-response",
  "version": "1.0.0",
  "description": "Limit number of responses on a form",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@oneblink/sdk": "^2.3.1"
  }
}
